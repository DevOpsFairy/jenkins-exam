apiVersion: v1
kind: Service
metadata:
  name: cast-service
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.cast.port }}
    targetPort: {{ .Values.cast.port }}
  selector:
    app: cast-deployment