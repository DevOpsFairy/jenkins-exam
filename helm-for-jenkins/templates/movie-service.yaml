apiVersion: v1
kind: Service
metadata:
  name: movie-service
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.movie.port }}
    targetPort: {{ .Values.movie.port }}
  selector:
    app: movie-service